#define FLEX_PIN 34  // ADC pin

void setup() {
  Serial.begin(115200);
}

void loop() {
  int value = analogRead(FLEX_PIN);

  Serial.print("Flex Value: ");
  Serial.println(value);

  delay(300);
}
